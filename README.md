# Setup
- Compile .java to .class and put into out/: `Ctrl + Shift + B` OR `Run Task: Build Task` OR `javac -d out src/com/wss/*.java`
- Run the thing: `Run Task: Run Java` OR `java -cp out com.wss.Main`